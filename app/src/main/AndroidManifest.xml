<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="messj.strengthenchemistry">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>

                <!--add below line for dropbox api working: mkymswyt61jbyym is app key-->
                <data android:scheme="mkymswyt61jbyym" />

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".chooserlistitem.ListItemActivity"></activity>
        <activity android:name=".ItemSelectActivity"></activity>
        <activity android:name=".listlessoncontent.ListContentActivity"></activity>
    </application>

</manifest>